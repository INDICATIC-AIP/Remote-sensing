# NASA API Configuration
# Get your API key from https://api.nasa.gov/
NASA_API_KEY=your_nasa_api_key_here

# CLI Download Configuration
DEFAULT_REGION=panama
DEFAULT_LIMIT=100

# Remote SSH Configuration (for remote downloads via ssh_download.sh)
SSH_HOST=your_remote_host_ip
SSH_PORT=2222
SSH_USERNAME=your_remote_username
SSH_PASSWORD=your_remote_password

# NAS Configuration
# Mount point where the NAS is mounted on your system
NAS_MOUNT=/mnt/nas_local
# Full path to the NAS data directory
NAS_PATH=/mnt/nas_local/DATOS API ISS

# IMPORTANT SECURITY NOTES:
# 1. Copy this file as .env: cp .env.example .env
# 2. Edit .env with your real credentials
# 3. Get NASA API Key at: https://api.nasa.gov/
# 4. NEVER commit .env to Git (it's already in .gitignore)
# 5. Use SSH keys instead of passwords when possible

